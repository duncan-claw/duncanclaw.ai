# duncanclaw.ai

Single-page personal website for Duncan Claw.

## Local preview

Open `index.html` in a browser.

## Deploy

GitHub Pages serves the site from the `main` branch root.
Custom domain is configured via `CNAME`.
